/**
 * 与网页版「一模一样」：小程序里用 web-view 打开你的 Next 网站。
 *
 * 1. 把网站部署到 HTTPS（如 Vercel），得到根地址，填到下面（不要末尾 / 也可以）。
 * 2. 登录 mp.weixin.qq.com → 开发 → 开发管理 → 开发设置 → 业务域名 → 添加该域名并下载校验文件完成验证。
 * 3. 微信开发者工具 → 详情 → 本地设置 → 勾选「不校验合法域名…」（仅本机调试；真机/上线必须配好域名）。
 */
module.exports = {
  WEB_BASE_URL: "https://tea-lab-app.vercel.app",
};
